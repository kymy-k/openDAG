# AGENTS.md

This repository uses a contract-first functional DAG workflow.

## Agent Roles

The agent acts as the master agent unless explicitly assigned one DAG node as a subagent.

- Master agent: owns DAG decomposition, contracts, public I/O, dependencies, architecture, subagent assignments, patch review, and full verification.
- Node subagent: implements or tests exactly one assigned node and may modify only files listed in that node's `allowedFiles`.

## Rules

1. Read the DAG/spec files before editing.
2. Do not implement before a contract and tests exist.
3. Every DAG node must have a separate test file under `tests/`, based on its spec, schemas, invariants, and dependencies.
4. Keep DAG nodes pure and deterministic.
5. Keep filesystem, network, database, time, randomness, process environment, and hidden global state outside pure nodes.
6. Master may edit DAG/contracts/architecture when required by the requested feature.
7. Subagents must not edit public interfaces, DAG entries, shared contracts, package scripts, verification tools, architecture, or other nodes.
8. Subagents must not edit outside the assigned node's `allowedFiles`.
9. Do not weaken tests to pass.
10. If a contract is impossible or underspecified, propose a contract change instead of silently modifying it.
11. Run node-level and full verification before finishing.
12. Explain what changed and how it was verified.
